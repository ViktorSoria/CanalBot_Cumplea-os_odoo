<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="purchase_order_form_view" model="ir.ui.view">
        <field name="name">purchase.order.form.view</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='button_done']" position="after">
                <button name="load_lines" type="object" string="Cargar Lineas" states="draft"/>
            </xpath>
        </field>
    </record>

    <record id="purchase_order_load_form_view" model="ir.ui.view">
        <field name="name">purchase.order.load.form.view</field>
        <field name="model">purchase.order.lines.load</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="purchase_id" invisible="1"/>
                    <group>
                        <group>
                            <field name="file_type" widget="radio"/>
                        </group>
                        <group>
                            <field name="file"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="product" widget="radio"/>
                        </group>
                        <group>
                            <field name="detalles" string="Cargar costos de" widget="radio"/>
                        </group>
                    </group>
                    <group>
                        <h3>
                            <p>Columnas del archivo:</p>
                            <p>Producto | Cantidad | Precio</p>
                        </h3>
                    </group>
                </sheet>
                <footer>
                    <footer>
                        <button name="generate_lines" type="object" class="oe_highlight" string="Aceptar"/>
                        <button class="oe_link" string="Cancelar" special="cancel"/>
                    </footer>
                </footer>
            </form>
        </field>
    </record>

        <record id="sh_message_wizard" model="ir.ui.view">
        <field name="name">sh.message.wizard</field>
        <field name="model">message.wizard</field>
        <field name="arch" type="xml">
            <form string="Message">
                <script>
                    $(document).ready(function(){
                    $('.modal-dialog').css({'width': '40%', });
                    });
                </script>
                <field name="name"/>
                <footer>
                    <button string="OK" class="btn-primary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="sh_message_wizard_invoice" model="ir.ui.view">
        <field name="name">sh.message.wizard</field>
        <field name="model">message.wizard.invoice</field>
        <field name="arch" type="xml">
            <form string="Message">
                <script>
                    $(document).ready(function(){
                    $('.modal-dialog').css({'width': '40%', });
                    });
                </script>
                <field name="invoice_ids" invisible="1"/>
                <field name="name"/>
                <footer>
                    <button string="Confirmar" class="btn-primary" type="object" name="confirm"/>
                    <button string="Cancelar" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

        <record id="confirm_view_form" model="ir.ui.view">
        <field name="name">confirm.view.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account_edi.view_move_form_inherit"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_process_edi_web_services']" position="attributes">
                <attribute name="context">{'return_message':True}</attribute>
            </xpath>
        </field>
    </record>


</odoo>
